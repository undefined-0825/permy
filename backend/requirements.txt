fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0

# settings
pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0
python-dotenv>=1.0,<2.0
openai>=1.0,<2.0

# DB (async SQLAlchemy + SQLite / PostgreSQL)
SQLAlchemy>=2.0,<3.0
aiosqlite>=0.19,<1.0
psycopg[binary]>=3.0,<4.0  # PostgreSQL driver for production

# Redis (optional; can be disabled by REDIS_DISABLED=true)
redis>=5.0,<6.0

# TimezoneInfo support
tzdata>=2024.1
