[Mobile App: Flutter]
  - Share受領: LINE txt URI -> history_text生成(トリム)
  - 設定UI -> /me/settings GET/PUT (ETag/If-Match)
  - 生成UI -> /generate (Idempotency-Key)
  - A/B/C表示 -> クリップボードコピー

        HTTPS (TLS)
            |
            v
[Backend: FastAPI]
  - /auth/anonymous  -> token発行 (Bearer/Opaque)
  - /me/settings     -> RDB(user_settings) + ETag
  - /generate
      - safety_gate (明確NGはAI呼ばず安全A/B/C)
      - usage_daily (JST日次制限)
      - idempotency (Redis)
      - OpenAI API (Structured Outputs)

            |
            +--> [Redis] session / rate / migration / idempotency
            |
            +--> [PostgreSQL] users / user_settings / usage_daily