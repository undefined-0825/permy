# docs/spec/10_product/product_spec.md — [ProjectName] プロダクト仕様（中核SSOT / MUST）

**Last Updated (JST):** 2026-03-07 12:30:00 +0900

> 目的：Copilotが迷わず実装できるよう、[ProjectName]の中核仕様（●●/入出力/●●/●●/NG/UX）を **曖昧さなく固定**する。  
> ●●（●●txt・●●）の保存禁止は **最優先**。本Spec内でも●●を扱う設計は行わない。

---

## 0. 参照優先順位（MUST）
- 参照起点：`docs/ssot/SSOT.md`
- 世界観SSOT：`docs/spec/00_world/world_concept.md`
- 運用・禁止：`docs/spec/01_rules/project_rules.md`
- プライバシー/ログ：`docs/spec/01_rules/privacy_logging.md`
- NG制御SSOT：`docs/spec/10_product/ng_policy.md`
- バックエンド設計：`docs/spec/20_backend/backend_spec.md`
- フロント設計：`docs/spec/30_frontend/frontend_spec.md`

本Specは上記と **矛盾しない**こと（矛盾がある場合は本Specを出さずに「確認事項」にする）。

---

## 1. プロダクト定義（MUST）
### 1.1 ゴール（内部KGI / MUST）
- **[PM]さんの●●化**（最短で●●・●●につながる状態を作る）。  
  ※ユーザー向け文言に「売上」「●●」「●●意識」等の●●表現を持ち込まない（世界観SSOT/NGポリシーに従う）。

### 1.2 ユーザー価値（MUST）
- ●●ユーザーの「●●/●●」●●●●●●、●● **●●** が「●●」として●●し、●●と●●を下げる。

### 1.3 絶対●●（MUST）
- **●●は提●●のみ**（●●しない。●●主体はユーザー）。
- **●●導線固定**：●●「●●を●●」→ `●●` を●●受信するのみ（●●は実装しない）。
- **●●/●●を保存しない**（端末・サーバ・ログ・テレメトリすべて）。
- 日本国内向け。Android / iPhone。

---

## 2. ●●・●●・●●（SSOT / MUST）
### 2.1 ●●・●●（MUST）
- ●●：**●● ●●**
- ●●：**1日●●**
- ●●：**1日●●**

> 日次●●はクライアント表示に依存せず **サーバ側で必ず判定**する（backend_spec参照）。

### 2.2 ●●の扱い（MUST）
- APIの外部互換の `●●` は `●●|●●` の2値（backend_spec参照）。
- ●●形態の内部区別：
  - `●●`: `●●|●●`
  - `●●`: `●●|●●|●●`
- **機能判定は ●● のみ**。`●●=●●` は API上 `●●=●●` として返す（●●区別はフロントに露出しない）。

### 2.3 Pro専用機能（MUST）
- ●●のみ：推定メーター（●●/●● 0..100）表示（backend_implの `meta.●●` を使用）
- ●●のみ：●●方針（コンボ）のうち **2/3/4/5** を実行可能（後述）
- ●●は●●専用機能をUI上に表示してよいが、選択/実行時は「有料版のみ」●●内（アップセル）を必ず出す。

---

## 3. 初期導線・アカウント・移行（MUST）
### 3.1 匿名開始（MUST）
- 初回は **匿名ユーザーID** で開始し、ユーザー●●の登録導線は作らない（離脱率上昇のため）。
- 起動時、token未保持なら `POST /api/v1/auth/anonymous` で token取得（frontend_spec / backend_spec参照）。

### 3.2 端末移行（MUST）
- QR移行は廃止。二段階承認は入れない。
- **移行コード12桁**で移行（期限あり / 1回限り / レート制限あり）。

### 3.3 アカウント削除（MUST）
- 削除導線は設定画面に配置し、確認ダイアログを必須とする。
- 削除実行は `DELETE /api/v1/auth/me` で行う。
- 削除対象はユーザー本体と設定・日次利用カウント等の関連メタ情報とし、●●●●/●●●●はもともと保存しない。
- 削除後は既存セッションを無効化し、同一トークンではAPIを利用できない状態にする。

---

## 4. ●●（●●）仕様（MUST）
### 4.1 ●●形式（MUST）
- `●●` ファイル（URI）を●●する。
- アプリ内の●●/●●は実装しない。

### 4.2 文字コード（SHOULD）
- UTF-8優先 + フォールバック（実装詳細はfrontend_impl/backend_impl側で確定）

### 4.3 トリム規則（MUST）
- **末尾（最新）優先**で●●る。
- ●●：**●● / ●●**  
- ●●：**●● / ●●**  
- 不足時は全行、超過時は末尾から文字数で切る。

### 4.4 UI上の根拠表示（MUST）
- ●●画面に「●●った●●」を **折りたたみ表示**（ユーザーが根拠を確認できる）。

---

## 5. ●●（●●/●●）仕様（SSOT / MUST）
### 5.1 二軸の定義（MUST）
- [ProjectName]は以下●●で●●を構成し、●●に反映する：
  - **●●Type**（●●：●●/●●）
  - **●●Type**（●●：●●）

### 5.2 Type定義（IDはenum名がSSOT / MUST）
ここは秘密

```

#### 5.2.1 互換性（MUST）
- 旧 `Flow` は廃止し `Balance` を採用する。
- 既存データに `Flow` がある場合、サーバは `Balance` に正規化して返す（クライアントは `Flow` を扱わない）。

### 5.3 ●●結果の保持（MUST）
- ●●結果は **1セットのみ**保持。再●●で上書きする。
- 保存先SSOT：`/api/v1/me/settings` の `settings_json`（端末ローカルに●●/●●結果を永続保存しない）。
- ●●・●●・●●アルゴリズムは `persona_scoring_spec.md` を正とする。

### 5.4 ●●結果画像（SSOT / MUST）
- 画像はアプリ同梱。サーバは画像を扱わない。
- 画像ファイル名はSSOT（命名変更/リネーム禁止）。

ここも秘密
---

## 6. Settings（/me/settings）スキーマ（SSOT / MUST）
> `settings_json` は「●●ではない設定メタ」のみ。●●保存禁止と矛盾させない。

### 6.1 必須キー（MUST）
ここはスキーマ設計になるので秘密

---

## 7. ●●（/generate）仕様（中核 / MUST）
### 7.1 返却形式（MUST）

7～12章もアプリの中核になるので秘密


---

## 13. 公開に必要な文書（MUST）
- 利用規約
- プライバシーポリシー
- ヘルプ（使い方）

---

14章もアプリの中核になるので秘密

---

## 15. 公開前TODO（法務/ストア審査の最低限）
- [x] 設定画面から遷移できる「利用規約」ページを実装（または外部URL導線を実装）。
- [x] 設定画面から遷移できる「プライバシーポリシー」ページを実装（または外部URL導線を実装）。
- [x] 設定画面から遷移できる「ヘルプ（使い方）」ページを実装。
- [x] Google Play / App Store の公開情報に、上記法務ページURLを登録。
- [x] ●●導線を出す場合は「購入復元」導線を実装し、ストア審査要件を満たす。
- [x] ●●導線を出す場合は「サブスクリプション管理（解約）」導線を実装。
- [x] アカウント削除/データ削除要件に備え、削除導線（UI）とAPI方針をSpecで確定。
- [x] OSSライセンス表示導線を設定画面に実装（Flutter標準のライセンス画面可）。

### 15.1.1 ●●機能実装●●（2026-03-07更新）
ここもアプリの中核になるので秘密

### 15.1.2 アカウント削除実装●●（2026-03-07追加）
ここもアプリの中核になるので秘密

### 15.1 備考
- 本TODOは「MVP機能」ではなく「公開審査・運用」観点の最低限チェック項目である。
- ●●機能を有効化しない段階でも、利用規約/プライバシーポリシー/ヘルプは必須とする。


