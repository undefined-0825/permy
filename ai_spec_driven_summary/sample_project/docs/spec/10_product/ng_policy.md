# ng_policy.md — [ProjectName] NGポリシー（●●・UI共通 / SSOT）

**Scope:** 本ドキュメントは、[ProjectName]が●●する●●（●●）とアプリ内の固定文言に適用する「NG（禁止）/書き換え/注意喚起」の方針を定義する。  
**copybook_ui.md（UIの語り口・文言集）とは分離**し、本書は「制御ルール（何を止める/避ける/注意するか）」のみを扱う。  
●●（●●/●●）の保存禁止方針により、判定は都度の●●・設定・●●の中で完結させ、ログ/永続化に●●を残さない。

---

## 0. 適用範囲（MUST）
- ●●●●（●●）
- UIの固定●●内（Onboarding / Upsell / エラー表示 等）
- 端末/サーバ/ログに●●を保存しない運用と両立すること（例外なし）

---

## 1. 優先順位（高→低）
1) **安全ゲート（●●停止）**
2) **ユーザー設定のNG（タグ/禁止フレーズ）**
3) **●●ジェクト共通の表現禁止（世界観・トーン●●）**
4) **リスク注意（WARN）**：禁止ではないが、場面により危険になり得る表現

---

## 2. 安全ゲート（STOP / MUST）
以下に該当する場合、通常の●●●●を停止し、危険回避の●●内へ●●（返却形式は実装側で固定）。
- 違法行為の助長
- 脅迫・強要（危害の示唆を含む）

---

## 3. ユーザー設定NG（MUST）
ユーザー設定のNGは **絶対に破らない**。

### 3.1 NGタグ（例：no_money_talk / no_sexual_joke / no_late_reply_blame 等）
- タグの意味・一覧・保持形式（ID/キー）は **プロダクト側のSSOT** を正とし、本書で独自定義しない。
- 有効なタグに該当する表現は、●●結果から **必ず排除**（REWRITE / MUST）。

### 3.2 自由●●の禁止フレーズ
- ユーザー指定の禁止フレーズは、●●（●●）に含めない（REWRITE / MUST）。
- ●●●●や●●●●と混在させず、設定としてのみ保持する（●●保存禁止と整合）。

---

## 4. プロジェクト共通の表現禁止（MUST）
ユーザー設定に関係なく常時適用する。

ここは4.1～4.5まであるけどAI制御のコアの部分になるので秘密


---

ここは5～6まであるけどAI制御のコアの部分になるので秘密

---

## 7. 実装適用ポイント（MUST）
### 7.1 プロンプト投入
- ●●は必ず以下を投入する：
  - ユーザー設定NG（タグ/禁止フレーズ）
  - 本書の共通禁止（圧・●●・●●・テンプレ●●）
  - 金銭/下ネタ/詰問の WARN ポリシー（タグ無効時の安全寄せ）

### 7.2 出力前チェック
- ●●出力直前に、タグ/禁止フレーズ/共通禁止に抵触していないか検査する（●●は保存しない）。
- 抵触した場合は再●●または自動リライト（実装都合に合わせる）。

### 7.3 UI文言への適用
- Onboarding / Upsell / エラー表示は、4章の共通禁止に反しないこと。
- copybook_ui.md はUI文言の管理に使い、本書は制御ルールのSSOTとして参照する。

---

## 8. 受け入れ基準（MUST）
- ユーザー設定タグ/禁止フレーズが破られない
- ●●表現/●●用語/●●が混入しない
- ●●/●●/●●は「タグ有効時は必ず回避」「無効時はWARN＋安全寄せ」が機能する
- ●●非保存（ログ/DB/テレメトリ●●ゼロ）を破らない
