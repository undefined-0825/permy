# docs/spec/00_world/ui_animations.md — UI演出仕様（●●/●●シークエンス / MUST）
**Last Updated (JST):** 2026-03-03 11:25:00 +0900

> 目的：[ProjectName]の世界観を「最小の演出」で成立させ、実装の迷い・仕様劣化・勝手な追加設定を防ぐ。  
> 注意：本ファイルは **演出のSSOT**。ここにない演出・文言・装飾は追加しない。

---

## 0. 参照（MUST）
- `docs/ssot/SSOT.md`
- `docs/spec/00_world/world_concept.md`
- `docs/spec/10_product/product_spec.md`
- `docs/spec/30_frontend/frontend_spec.md`
- `docs/spec/01_rules/project_rules.md`

---

## 1. 演出の基本方針（MUST）
- 演出は **短く・分かりやすく・邪魔にならない**（待たせない）。
- 余計な●●用語/専門用語/追加設定を入れない。
- “●●●●”や●●表現は禁止（world_concept参照）。
- 演出中でも **●●（●●/●●●●）をログに出さない**。

---

## 2. 色彩（MUST）
- 通常時：**淡い●●**（●●感）
- ●●：背景を **●●**へ反転（●●モードへの切り替え）

### 2.1 色の具体値（未確定）
- 淡い●●/●●のHexは別途確定する（未確定扱い）。
- ただし「淡い●●→●●反転」という **状態遷移**自体は確定。

---

## 3. シークエンス（MUST）
対象：ユーザーが「●●」ボタンを押してから、●●を表示するまで。

### 3.1 ●●開始（t=0）
- UIをロック（二重●●防止）
- 背景を淡い●●から●●へ **クロスフェード**（●●的に切り替えを明確化）

### 3.2 ●●表示（固定文言 / MUST）
背景反転と同期して、以下の2文を順に表示する。  
**文言は一字一句固定**（変更禁止）。

1) 「●●●●」  
2) 「●●●●」

#### 表示ルール（MUST）
- 表示位置：画面中央〜やや下（Safe Area内）
- 文字サイズ：読みやすさ優先（端末に合わせたレスポンシブ）
- 1文目→2文目への切替はフェードでよい（派手な●●禁止）
- ●●が極端に速い場合でも **最低1文目は表示される**（0msで飛ばない）

### 3.3 ●●変容（MUST）
- 背景が●●へ反転する瞬間に、●●の●●が **●●の●●へ●●に変容**する。
- 変容は「粒子崩壊」等の過剰表現は避け、**シンプルなモーフィング**でよい。
- 指示にない装飾（例：●●化など）を勝手に入れない。

### 3.4 完了（●●表示）
- シークエンス完了後は **●●を表示しない**（結果表示のみ）
- ●●を表示（product_spec準拠）
- UIロック●●（必要な操作のみ可能に）

---

## 4. 時間（Duration）規定（MUST）
> ネットワーク待ち時間を演出で隠すのではなく、短く世界観を出す。

- 背景反転：200〜350ms
- ●●1：350〜700ms（最低350ms）
- ●●2：350〜700ms（最低350ms）
- ●●変容：背景反転と同時に開始し、最大700msで完了

### 4.1 ネットワークが遅い場合（MUST）
- ●●2の後、ローディング表示へ移行してよい（簡素なスピナー）
- 追加の世界観文言は禁止（2文のみ）

---

## 5. アクセシビリティ（MUST）
- OSの「●●ーションを減らす/Reduce Motion」が有効なら：
  - 背景反転を瞬時（0〜100ms）にする
  - ●●変容は静的な切り替え（フェード）でもよい
- 文字のコントラストを確保（●●背景に対して視認性確保）
- 画面読み上げ（TalkBack/VoiceOver）を阻害しない（●●は読み上げてもよい）

---

## 6. 失敗時（MUST）
### 6.1 API失敗
- 背景を淡い●●に戻す（200〜350ms）
- エラーは `error_code` + 短い説明のみ（●●なし）
- 再試行可能にする（UIロック●●）

### 6.2 ●●●●失敗
- 演出は開始しない
- 「●●を●●してください」などの短い●●内（●●誘導禁止）

---

## 7. 禁止事項（NG）
- 固定2文以外の世界観●●追加
- ●●用語、●●表現（●●等）
- 装飾の勝手追加（●●を金にする等）
- 長すぎる演出（ユーザーを待たせる）
- 演出中に●●/●●をログ出力すること

---

## 8. 未確定 / 確認事項（最大3点）
1) 「淡い●●」のHex確定（ブランドカラーとして固定するか）。  
2) ●●素材の形式（Lottie/PNG連番/SVGモーフ）をfrontend_implで確定する必要がある。  
3) ローディングスピナーのスタイル（最小で良いが、●●背景で視認性を担保）。  
