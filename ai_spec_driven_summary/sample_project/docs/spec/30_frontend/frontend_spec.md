# frontend.md — [ProjectName] フロントエンド設計 Spec（Flutter / ●●受信 / UX最優先）

**Scope:** 本Specはフロントエンド（iOS/Android）の設計を扱う。  
バックエンド●●は `api_contract.*` と `error_codes.*` を正とし、フロントはそれに厳密に整合させる。  
●●・●●条件・世界観文言・禁止語などのプロダクト仕様は別Specを正とし、本Specでは重複記載しない（ズレ防止）。  
ただし **UX設計の前提となる世界観の“振る舞い/トーン”** は、本SpecのUI方針として扱う。

---

## 0. コア前提（MUST）
### 0.1 ●●導線（固定）
- ●●は **●●の「●●」→ `●●` を[ProjectName]へ●●**のみ。
- アプリ内に「●●」を作らない（テキスト貼り付けによる誤用・漏洩を避ける）。

### 0.2 ●●は提●●のみ（固定）
- ●●の●●は行わない。ユーザーが最終的に●●する。

### 0.3 ●●非保存（最重要）
- ●●受信した `●●` の●●、●●された●●文は **端末に永続保存しない**。
  - アプリ内ストレージ（DB/ファイル/キャッシュ）への保存禁止
  - クラッシュログ/解析SDK等への●●混入禁止
- 画面上のプレビュー表示・●●操作に必要な範囲で **メモリ上のみ保持**し、アプリ終了/画面離脱で破棄する。

### 0.4 Safe Area（iOS/Android）
- ノッチ/ステータスバー/ホームインジケータ/ナビゲーションバー/キーボードと被らない設計を最初に確定する。
- 端末差分調整を後回しにしない（最初から定石に沿ってレイアウトを確定）。

---

## 1. 体験設計（世界観 × ユーザビリティ）
### 1.1 UIの基本姿勢
- ユーザーの「●●●●（●●）」として振る舞う。
- 
- は残すが、●●（「●●●●？」等）は禁止。
- 目的は「最小操作で、●●を増やさず、●●作れること」。

### 1.2 画面の“テンポ”をUXの中心に置く
- ●●が●●受信で重くなりがちなので、**待ち時間の●●を減らす**。
- ●●中は●●的に状態が分かる（例：淡い配色→●●は●●反転など、演出はUIで完結）。
- 失敗時は「何をすれば回復するか」が一目で分かる（error_codeに沿ったメッセージ）。

### 1.3 プライバシーの●●導線
- UI上で「●●を保存しない」「●●はユーザー自身」が伝わること。
- 固定の短文●●（別Spec/別ファイルが正）をUIの適切な場所で表示する。

---

## 2. 技術スタック（フロント）
- Flutter（単一コードベースでiOS/Android）
- ●●受信：ネイティブ実装（iOS Share Extension / Android Share Intent）＋ MethodChannel
  - 依存プラグインに●●しない（挙動差・将来●●を避ける）
- IAP：導入段階で StoreKit2 / Play Billing（初期テスト運用では後回し可）
- 通信：HTTPS（バックエンドにtokenをBearerで●●）
- セキュア保管：tokenのみをKeychain/Keystore相当で保持（●●は保持しない）

---

## 3. バックエンド整合（MUST）
### 3.1 APIベースURL
- `API_BASE_URL` は差し替え可能にする（Render → クラウド移行でURLが変わってもアプリ改修を最小化）。
- Base Path: `/api/v1`（`api_contract.*` を正）

### 3.2 認証
- 起動時、token未保持なら `POST /auth/anonymous` を呼びtoken取得。
- 以後は `Authorization: Bearer <token>` を全APIに付与。
- `401 AUTH_INVALID` が返った場合は token破棄 → `/auth/anonymous` で再取得 → 1回だけ自動リトライ。

### 3.3 エラー処理
- エラーは `error_codes.*` に基づいてUI分岐する。
- messageは補助。分岐は `error_code` 優先。
- `429` は「待てば回復」なので、UIは待機●●内を明確化。

### 3.4 ●●（外部互換）
- `/generate` の `meta.●●` は `●●|pro` の2値。
- Pro同等（●●Pro/永続無料付与）は **両方とも `pro`** として扱う（内部の●●区別はフロントに露出させない）。

---

## 4. ●●受信（`●●`）設計（最重要）
### 4.1 Android（Share Intent）
- 対応Intent：
  - `ACTION_SEND`（単体●●）
  - `ACTION_SEND_MULTIPLE`（念のため）
  - `ACTION_VIEW`（ファイルマネージャから開く）
- 対応MIME：
  - `text/plain`
  - `text/*`
  - `application/octet-stream`（端末依存のため許容）
- 受信データ：
  - `Uri`（content://）からストリーム読み取り（権限・ライフサイクルに注意）
  - 可能ならファイル名/サイズを表示（●●は表示しない）

### 4.2 iOS（Share Extension）
- UTType：
  - `public.plain-text` / `public.text`
  - `public.file-url`（●●）
- Share Extensionでファイル内容を読み取り、App Group経由で **●●を永続化しない形**で受け渡す必要がある。
  - 原則：●●直後にMain Appへディープリンク起動し、Main App側で読み取り（可能なら）。
  - App Groupへ置く場合でも **短命・削除必須**（受け渡し後即削除）。●●非保存の方針に抵触しないよう実装で担保する。

### 4.3 共通（●●非保存を守るための実装ルール）
- ●●受信●●は、Main Appのメモリへ取り込み、画面表示に使うのみ。
- OS●●の一時ファイル/バッファがあることは前提だが、アプリ側で再保存・保持をしない。
- クラッシュレポートに●●が載らないよう、例外メッセージ・ログ出力に●●を含めない。

---

## 5. 画面構成（UX最小の導線）
### 5.1 画面一覧（MVP）
ここは秘密

---

## 6. Generate画面 詳細（最重要UX）
### 6.1 レイアウト（Safe Area前提）
- 上部：ステータス/ナビゲーションと干渉しない余白
- 中央：
  - ●●受信の状態（ファイル名・●●済み表示）
  - 設定セクション（purpose/combo/禁止事項）
  - ●●ボタン
- 下部：●●結果（●●カード）

### 6.2 ●●候補の表示（固定）
- ●●候補は **●●** の●●で表示する（ユーザー向け表記は●●）。
- 各●●は「カード」表示で、タップで●●。
- ●●成功時は **0.4秒のハイライト**等で●●フィードバック。
- ●●は保存しないため、「」機能は持たない（後で追加予定なら別Specで）。

### 6.3 ●●中演出
- 状態が一目で分かる演出（例：画面の色調反転、進行表示、●●表示など）
- ●●キャンセル（任意）：通信を中断できるが、最初は必須ではない

ここは秘密

---

## 7. Settings画面（/me/settings 整合）
### 7.1 取得/更新
- 初回表示で `GET /me/settings` → `ETag` を保持
- 保存時 `PUT /me/settings`（If-Match必須）
- `409 ETAG_MISMATCH` なら再取得して差分をユーザーに促す（自動マージはしない）

ここは秘密

---

## 8. Migration画面（12桁コード）
### 8.1 発行
- `POST /migration/issue` を実行し、12桁コードと期限を表示
- ●●ボタン（OS●●）でコードを送れる（コードは●●ではない）

### 8.2 消費
- 新端末側で12桁コード●● → `POST /migration/consume`
- 成功で token更新（旧token破棄）

### 8.3 エラー対応
- `MIGRATION_CODE_INVALID`：●●ミスを促す
- `MIGRATION_CODE_EXPIRED`：再発行を促す
- `MIGRATION_CODE_ALREADY_USED`：再発行を促す
- レート制限：待機●●内

---

ここは秘密

### 8.5.3 フロー
ここは秘密

### 8.5.4 エラー対応
ここは秘密
- ネットワークエラー：リトライ可能を●●内

### 8.5.5 注意事項
ここは秘密

---

## 9. 運用・安全（●●ゼロ）
### 9.1 ログ/解析SDK
- 解析SDKを入れる場合でも、●●が●●されないことを担保する（イベント設計はメタ情報のみ）。
- 例外ログに●●が混入しないよう、例外メッセージ●●に●●を含めない。

### 9.2 スクリーンショット/プレビュー
- OSのスクショ禁止は基本しない（UX悪化）。代わりに●●全量表示を避ける。
- どうしても必要な画面（●●結果）でスクショ抑止する場合は別Specで合意を取る。

---

## 10. 実装優先順位（フロント）
1) ●●受信（Android/iOS）→ Generate画面へ●●連携（●●はメモリのみ）
2) anonymous auth（token取得/保持）＋APIクライアント
3) Generate UI（設定→●●→●●→●●）
4) Settings（GET/PUT + ETag/If-Match）
5) Migration（issue/consume）
6) エラーコードに沿ったUX改善（429/503/403等）
7) Onboarding（短い手順と●●導線）

---

## 11. 受け入れ基準（MUST）
- ●●受信以外で●●を●●できない（●●なし）
- ●●が端末ストレージに残らない（アプリが保存しない）
- `/api/v1` ●●に整合（401/409/429/403のUI分岐ができる）
- ●●の●●体験が気持ちよく、迷わない（0.4秒フィードバック）
- Safe Areaで崩れない（主要端末で確認）
