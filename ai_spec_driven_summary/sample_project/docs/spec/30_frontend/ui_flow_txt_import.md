# ui_flow_txt_import.md — UI Flow: ●●●●（●●）取り込み（●●受信）Spec

**Scope:** 本ドキュメントは「●●の●●で得られる `●●` を[ProjectName]へ●●して取り込む」UIフローを定義する。  
本フローは **●●導線の唯一の正**（●●等は作らない）。  
バックエンド●●は `api_contract.*` と `error_codes.*` を正とし、●●非保存方針は全画面で例外なし。

---

## 0. 前提（MUST）
- ●●は **●●の「●●」→ `●●` を[ProjectName]へ●●**のみ。
- アプリ内に貼り付け●●欄を作らない。
- `●●` ●●は **端末・サーバに保存しない**（ログにも出さない）。
  - UI表示はメモリ上のみ（画面離脱/アプリ終了で破棄）。
- ●●は提●●のみ（●●なし）。
- iOS/Androidともに **●●受信**（iOS Share Extension / Android Share Intent）で成立させる。

ここから先は秘密
