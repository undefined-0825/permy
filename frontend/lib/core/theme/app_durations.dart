/// Permy用アニメーション時間定義
class AppDurations {
  static const short = Duration(milliseconds: 150);
  static const medium = Duration(milliseconds: 200);
  static const long = Duration(milliseconds: 250);
}
